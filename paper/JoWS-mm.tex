%%
%% Copyright 2007-2025 Elsevier Ltd
%% This file is part of the 'Elsarticle Bundle'.
%% Template article for Elsevier's document class `elsarticle'
%% with numbered style bibliographic references
%%
\documentclass[preprint,12pt]{elsarticle}

\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{float}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{placeins}
\usepackage{tabularx}
\usepackage{xcolor}

\journal{Journal of Web Semantics}

\newcommand{\pending}{\textit{pending}}
\newcommand{\rdf}[3]{(\texttt{#1}, \texttt{#2}, \texttt{#3})}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}

\definecolor{mdbackground}{RGB}{248,249,251}
\definecolor{mdheading}{RGB}{0,92,175}
\definecolor{mdaccent}{RGB}{127,31,133}
\definecolor{mdstring}{RGB}{0,110,91}
\definecolor{ttlkeyword}{RGB}{0,92,175}
\definecolor{ttlresource}{RGB}{127,31,133}
\definecolor{ttlclass}{RGB}{179,92,0}
\definecolor{ttlproperty}{RGB}{0,110,91}
\definecolor{ttlstring}{RGB}{150,64,8}
\definecolor{ttlcomment}{RGB}{105,112,122}

\lstset{
    basicstyle=\ttfamily\footnotesize,
    breaklines=true,
    columns=fullflexible,
    keepspaces=true,
    showstringspaces=false
}

\lstdefinestyle{markdownstyle}{
    basicstyle=\ttfamily\scriptsize,
    backgroundcolor=\color{mdbackground},
    frame=single,
    framerule=0.3pt,
    rulecolor=\color{black!20},
    framesep=2pt,
    stringstyle=\color{mdstring},
    emph={Role,Task,Requirements,Schema,Structure,Rules,Ontology,Resource,Naming,Triples,Constraints,Concept,Nodes,Relationships,Tables,Figures,Checklist,Inputs,Evaluation,Judging,Scoring,Return,Output,Model,Judge},
    emphstyle=\bfseries\color{mdheading},
    aboveskip=4pt,
    belowskip=4pt,
    breaklines=true,
    columns=fullflexible,
    keepspaces=true,
    showstringspaces=false
}

\lstdefinestyle{turtlestyle}{
    basicstyle=\ttfamily\scriptsize,
    backgroundcolor=\color{mdbackground},
    frame=single,
    framerule=0.3pt,
    rulecolor=\color{black!20},
    framesep=2pt,
    sensitive=true,
    alsoletter={:@},
    morestring=[b]",
    stringstyle=\color{ttlstring},
    morecomment=[l]\#,
    commentstyle=\itshape\color{ttlcomment},
    morekeywords={@prefix,a},
    keywordstyle=\bfseries\color{ttlkeyword},
    morekeywords=[2]{:AreaChart,:Axis,:BarChart,:Branches,:DataPoint,:DataStore,:Dashed,:Decision,:Delay,:DependsOn,:Edge,:EndEvent,:Follows,:Gateway,:LineChart,:Node,:Process,:Solid,:Start,:StartEvent,:Task,:Terminator},
    keywordstyle=[2]\bfseries\color{ttlclass},
    morekeywords=[3]{:belongsTo,:label,:relationshipType,:relationshipValue,:shape,:source,:target,:title,:xAxis,:xValue,:yAxis,:yValue},
    keywordstyle=[3]\color{ttlproperty},
    morekeywords=[4]{:Chart,:Edge5,:Edge6,:DataPoint1,:DataPoint66,:Node1,:Node5,:Node6,:Node9,:XAxis,:YAxis},
    keywordstyle=[4]\color{ttlresource},
    aboveskip=0pt,
    belowskip=0pt,
    breaklines=true,
    columns=fullflexible,
    keepspaces=true,
    showstringspaces=false
}

\begin{document}

\begin{frontmatter}

\title{Image-to-RDF Knowledge Graph Construction from Scientific Visualizations using Large Vision-Language Models}

\author[inst1]{Beichen Wang\corref{cor1}}
\ead{beichen.wang@wur.nl}
\author[inst2]{Ali Mamanpoosh}
\author[inst3]{Carmel Suchard}
\ead{o.suchard@uu.nl}
\author[inst1]{Anna Fensel}
\ead{anna.fensel@wur.nl}
\cortext[cor1]{Corresponding author.}
\affiliation[inst1]{organization={Artificial Intelligence Chair Group},
             addressline={Wageningen University \& Research},
             city={Wageningen},
             postcode={6708 PB},
             country={The Netherlands}}
\affiliation[inst2]{organization={Department of Computer Engineering},
             addressline={University of Isfahan},
             city={Isfahan},
             country={Iran}}
\affiliation[inst3]{organization={Utrecht University},
             city={Utrecht},
             country={The Netherlands}}

\begin{abstract}
% Scientific knowledge is frequently communicated through visual artifacts such as charts, tables, and flow diagrams. Although these artifacts often contain compact empirical or procedural knowledge, they are difficult to reuse in Semantic Web applications because their content is encoded visually rather than as machine-interpretable triples. This paper studies image-to-knowledge-graph construction as a direct image-to-RDF task. We design a prompting-based framework in which a large vision-language model receives an infographic image and returns an RDF/Turtle graph. We construct two labelled image-to-RDF resources for evaluation: a VisText-derived chart-to-RDF dataset covering train, evaluation, and test splits, and a Diagram2Graph-derived diagram-to-RDF dataset obtained by expressing the original graph annotations in a lightweight RDF schema. We evaluate zero-shot, one-shot, dynamic one-shot, and few-shot prompting strategies where applicable, using both exact graph-matching metrics and softer graph-similarity metrics. For unlabelled scientific images, we introduce an LLM-as-a-judge framework adapted from KGEval and validate it on the two labelled datasets before applying it to an unlabelled domain application. The study provides datasets, methods, and an evaluation framework for assessing direct RDF extraction from scientific images under both labelled and unlabelled conditions.
\end{abstract}

\begin{keyword}
Image-to-knowledge-graph \sep RDF \sep Vision-language models \sep Multimodal \sep LLM-as-a-judge
\end{keyword}

\end{frontmatter}

\section{Introduction}\label{sec:intro}

Scientific communication relies extensively on visual artifacts. Charts summarize quantitative trends, tables arrange measurements and qualitative categories, and diagrams describe procedural or causal relations. However, such artifacts are often weakly represented, or entirely omitted, when scientific documents are converted into machine-readable corpora. In a typical retrieval-augmented generation workflow, for example, documents are first parsed into textual chunks or other retrievable representations before downstream question answering or analysis is performed \citep{rag_survey}. Visual elements whose meaning is encoded through marks, axes, layout, and topology can therefore be filtered out during preprocessing, even when they contain essential empirical or procedural knowledge. This omission limits both corpus construction and downstream structured knowledge acquisition from scientific literature.

Knowledge graphs provide a principled way to address this gap by representing entities, relations, and attributes in graph-structured form \citep{hogan2021knowledgegraphs}. RDF represents such information as subject-predicate-object triples, while ontologies define the classes and relations that make these triples interpretable and reusable. In the Semantic Web setting, RDF graphs can be validated syntactically, queried, linked to external resources, and integrated across domains \citep{rdf11concepts,turtle}. Directly converting scientific visual artifacts into RDF therefore offers a route from overlooked image-based evidence to explicit, queryable, and interoperable knowledge.

Existing research addresses important parts of this problem, but not the complete image-to-RDF task. Chart-understanding benchmarks and models focus on chart question answering, chart-to-text generation, or data recovery \citep{chartocr,chartqa,vistext,unichart,chartllama}. Diagram-understanding work can extract graph-like representations, but the target format is usually JSON, Mermaid, or another task-specific serialization rather than RDF \citep{diagram2graph,flowlearn}. Table-understanding systems increasingly process table images directly, but they commonly return reconstructed tables or task-specific answers \citep{mmtable,tabpedia}. Conversely, Semantic Web table interpretation typically assumes that tabular data are already available in textual or structured form \citep{semtab2022,semtab2024}. These lines of work motivate direct image-to-RDF construction, but they do not provide an end-to-end framework for producing RDF knowledge graphs directly from heterogeneous scientific images.

This paper studies such an end-to-end formulation. We use Gemini 3 Flash \citep{googledeepmind2025gemini3flashcard} as a large vision-language extraction engine and constrain its output to RDF/Turtle under predefined schemas. The framework is prompt-based and does not require task-specific fine-tuning. This design avoids decomposing visual interpretation, graph construction, and RDF serialization into independently optimized stages, a choice that is consistent with evidence from information extraction that pipeline architectures can propagate upstream errors, whereas joint models can reduce this dependency and improve extraction performance \citep{li2017neuraljoint}. We compare zero-shot, one-shot, few-shot, and dynamic one-shot prompting where applicable.

The main contributions are as follows:
\begin{itemize}
    \item We propose an end-to-end prompting framework for constructing RDF knowledge graphs directly from scientific visual artifacts, including charts, tables, and diagrams. The framework supplies the ontology in the prompt, optionally provides image-to-RDF demonstrations, and requires parseable Turtle output.
    \item We construct two labelled image-to-RDF datasets for systematic evaluation. From VisText \citep{vistext}, we derive RDF graphs that encode chart type, title, axis titles, and data points across the full train, evaluation, and test splits, while using only the test split for LLM evaluation. From Diagram2Graph \citep{diagram2graph}, we transform diagram annotations into a compact RDF schema that preserves node-edge semantics.
    \item We develop an LLM-as-a-judge framework for evaluating image-to-RDF outputs when reference graphs are unavailable. The framework adapts the rubric-based assessment idea of KGEval \citep{kgeval}, supports direct and pairwise judging modes, and is validated on the two labelled datasets before being applied to an unlabelled soil-health image collection.
\end{itemize}

Taken together, these contributions establish a reproducible experimental setting for image-to-RDF extraction. The paper characterizes the capabilities and limitations of prompt-based RDF generation, provides labelled resources for systematic evaluation, and offers a validation pathway for judge-based assessment when ground truth is unavailable.

The remainder of the paper is organized as follows. Section~\ref{sec:related} reviews related work on multimodal understanding, LLM-supported knowledge engineering, and LLM-as-a-judge evaluation. Section~\ref{sec:method} defines the image-to-RDF task, introduces the labelled datasets, and describes the prompting strategies. Section~\ref{sec:evaluation} presents the evaluation protocol and labelled-dataset results. Section~\ref{sec:discussion} discusses the observed performance patterns and limitations. Section~\ref{sec:soil_use_case} presents the unlabelled Soil-Health application, and Section~\ref{sec:conclusion} concludes the paper.

\section{Related Work}\label{sec:related}

\subsection{Multimodal Understanding of Charts, Tables, and Diagrams}

Chart understanding has been studied through data extraction, question answering, captioning, and chart-specific foundation models. ChartOCR recovers data from chart images by combining visual detection and post-processing \citep{chartocr}. ChartQA introduced a benchmark for chart question answering that requires both visual and logical reasoning \citep{chartqa}. VisText formulates semantically rich chart captioning over chart images and chart-derived information \citep{vistext}. UniChart and ChartLlama further demonstrate that chart-specific pretraining and instruction tuning can improve multimodal chart reasoning \citep{unichart,chartllama}. These studies show substantial progress in chart perception, but their primary targets are captions, answers, or recovered data tables rather than RDF graphs.

Visual table understanding similarly focuses on reconstructing table structure or answering questions over table images. Recent work such as Multimodal Table Understanding and TabPedia studies large vision-language models for table perception and comprehension \citep{mmtable,tabpedia}. These systems are relevant because table images are one visual source considered in this paper, but their target outputs differ from the RDF/Turtle graphs required for Semantic Web integration.

Diagram and flowchart understanding is closer to graph construction because the visual artifact itself often represents nodes, edges, and labelled transitions. Diagram2Graph extracts structured graph data from process and flow diagrams into JSON \citep{diagram2graph}. FlowLearn evaluates large vision-language models on flowchart understanding and topology reconstruction \citep{flowlearn}. Work on circuit diagrams also emphasizes that topological extraction is difficult and can benefit from modular visual parsing pipelines \citep{circuitdiagram24}. Our work differs by normalizing diagram outputs into RDF/Turtle and evaluating the same image-to-RDF interface across multiple visual genres.

\subsection{LLM-Empowered Knowledge Engineering}

Knowledge engineering includes ontology modelling, entity and relation extraction, schema mapping, graph construction, refinement, and evaluation. Recent work argues that LLMs and knowledge graphs are complementary: knowledge graphs provide explicit symbolic structure, while LLMs provide flexible language understanding, generation, and reasoning capabilities \citep{pan2024llmkgroadmap}. LLMs have therefore been studied as extractors or assistants for constructing graph fragments under predefined schemas and for supporting RDF construction or ontology mapping \citep{ghanemcruz2025kgc,mavridis2025rdfkg}. However, most such work still assumes textual or already structured inputs, including semantic table interpretation benchmarks such as SemTab \citep{semtab2022,semtab2024}. Multimodal knowledge graph research also links images and text to graph entities \citep{richpedia,multimodalkgsurvey}, but this differs from constructing RDF triples from the semantic content of an image itself.

A small number of recent studies move closer to image-based knowledge graph construction. For example, ImgGraph-LLM constructs graph triples from individual images through self-supervised data generation and LLM fine-tuning \citep{chen2025pixels}. This shows that image-to-graph construction is becoming a research topic in its own right. However, existing work remains limited with respect to direct RDF/Turtle generation from scientific visualizations under explicit ontological schemas. In this paper, the image itself is the source document from which RDF triples must be extracted. The resulting graph describes the semantic content of the image rather than attaching image resources to existing graph entities.

\subsection{LLM-as-a-Judge and Knowledge Graph Evaluation}

LLM-as-a-judge methods use language models to assess generated outputs when human evaluation is costly or when automatic metrics are incomplete. Surveys of this area describe LLM-based judging as a rapidly expanding evaluation paradigm, but also emphasize that reliability depends on task design, calibration, bias control, and validation against external evidence or human judgments \citep{gu2024llmjudge}. MT-Bench and Chatbot Arena showed that model-based judgments can be useful for open-ended language-model evaluation, while also raising concerns about bias and calibration \citep{mtbench}. For knowledge graphs, KGEval proposes using LLMs to evaluate knowledge graph construction quality through criteria such as relevance, correctness, informativeness, and coherence \citep{kgeval}.

This paper follows the same general principle but adapts the rubric to image-to-RDF extraction. The judge receives the source image and the candidate RDF/Turtle graph, and it is asked to assess whether the graph is faithful, informative, coherent, and specific with respect to the visible image content. We also avoid using the same model family for generation and judging. This design choice is motivated by evidence that LLM judges can exhibit self-bias and family-bias, assigning overly favourable ratings to outputs produced by themselves or by related models \citep{spiliopoulou2025playfavorites}. Using a different model family for the judge does not remove all evaluation bias, but it reduces one avoidable source of confounding. Because judge-based evaluation can itself be unreliable, we first validate it on labelled datasets before using it for unlabelled scientific images.

\section{Materials and Methods}\label{sec:method}

Figure~\ref{fig:overview} summarizes the overall study design, including the labelled and unlabelled datasets, the prompting-based image-to-RDF workflow, and the complementary evaluation settings used throughout the paper.

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{img/overview_figure_main.pdf}
\caption{Overview of the proposed image-to-RDF framework. Panel A summarizes the two labelled benchmarks used for metric-based evaluation. Panel B shows the extraction workflow, in which scientific images are processed with prompting strategies from zero-shot to few-shot using Gemini 3 Flash, followed by RDF/Turtle validity checking. Panel C shows evaluation on labelled datasets through graph-matching metrics and LLM-as-a-judge validation. Panel D separates the Soil-Health application setting, where no reference RDF graphs are available and prompting strategies are compared through the validated LLM-as-a-judge framework.}
\label{fig:overview}
\end{figure}

\subsection{Task Definition and Target Representation}

Let $x$ denote an input image and let $\mathcal{T}(x)=\{(s,p,o)\}$ denote the RDF triple set generated for that image. The task is to produce a Turtle serialization of $\mathcal{T}(x)$ that faithfully represents the information visible or directly implied by the image. The output is not a caption or a natural-language explanation. It is a knowledge graph fragment whose classes, entities, and predicates are defined by a dataset-specific schema.

This study focuses on two labelled image families: statistical charts and flow/process diagrams. For charts, the target graph captures the chart type, title, axes, and data points. For diagrams, the target graph captures nodes, visible node labels, node types, directed edges, edge labels, and edge semantics. In all cases, syntactic RDF/Turtle validity is treated as a prerequisite for evaluation because malformed graphs cannot be used directly in RDF tooling.

\subsection{Labelled Image-to-RDF Datasets}\label{subsec:datasets}

Table~\ref{tab:dataset_overview} summarizes the labelled datasets used for graph-matching evaluation. The VisText-derived resource contains RDF conversions for the full train, evaluation, and test splits, while the LLM prompting experiments use only the test split. Diagram2Graph is used as a labelled diagram-to-RDF benchmark.

\begin{table}[ht]
\centering
\small
\begin{tabular}{llll}
\toprule
\textbf{Dataset} & \textbf{Visual type} & \textbf{Raw items} & \textbf{Usable RDF graphs} \\
\midrule
VisText train & Charts & 7057 & 6869 \\
VisText eval & Charts & 883 & 855 \\
VisText test & Charts & 882 & 868 \\
Diagram2Graph & Flow/process diagrams & 219 & 219 \\
\bottomrule
\end{tabular}
\caption{Labelled image-to-RDF datasets used for evaluation. VisText contains RDF conversions for all original splits; LLM evaluation uses only the test split. Usable RDF graphs are those retained after excluding source items whose original labels were too noisy or incomplete to support reliable RDF reference construction.}
\label{tab:dataset_overview}
\end{table}

\subsubsection{VisText-derived Chart-to-RDF Dataset}
VisText was originally introduced for semantically rich chart captioning \citep{vistext}. We derive a chart-to-RDF dataset by defining a compact reference graph for each usable chart image. The target graph contains one chart resource typed as a bar, line, or area chart; the visible chart title; x-axis and y-axis resources with their visible titles when present; and one datapoint resource for each observation represented in the plot. Each datapoint records its x-value, y-value, and membership in the chart.

The VisText task requires recovery of both explicit textual elements and visually encoded quantitative content. This distinction is especially important for line and area charts. In many such images, individual datapoints are not directly labelled; the model must infer the intended number of observations from the chart title, axis titles, tick marks, temporal range, and plotted marks, and must then estimate the corresponding values from the visual geometry of the plot area. In the case shown in Figure~\ref{fig:vistext_example}, the model must infer from the chart title that the area chart should contain one datapoint for each year in the reported period.

The VisText target schema and a representative chart-to-RDF example are jointly shown in Figure~\ref{fig:vistext_example}. The schema is intentionally small so that evaluation focuses on chart semantics and data values. Not every original VisText item yields a reliable RDF reference graph. Some original labels contain damaged categories, inconsistent tabular annotations, or insufficient information to recover the plotted data. Such items are excluded from labelled evaluation, while the remaining items are released as RDF/Turtle references.

\begin{figure}[H]
\centering
\begin{minipage}[ht]{0.45\textwidth}
\vspace{0pt}
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{tabularx}{\linewidth}{@{}lX@{}}
\toprule
\textbf{Component} & \textbf{Vocabulary / target} \\
\midrule
Chart type & \texttt{:BarChart}, \texttt{:LineChart}, \texttt{:AreaChart} \\
Chart & \texttt{:Chart} with \texttt{:title}, \texttt{:xAxis}, \texttt{:yAxis} \\
Axis & \texttt{:XAxis}, \texttt{:YAxis}, \texttt{:Axis} \\
Data & \texttt{:DataPoint}, \texttt{:xValue}, \texttt{:yValue}, \texttt{:belongsTo} \\
\bottomrule
\vspace{1pt}
\end{tabularx}
\centering
\includegraphics[width=\linewidth]{img/vistext_example.png}
\end{minipage}\hfill
\begin{minipage}[ht]{0.5\textwidth}
\vspace{0pt}
\begin{lstlisting}[style=turtlestyle]
@prefix : <http://example.org/vistext#> .

:Chart a :AreaChart ;
    :title "Number of operational nuclear
            reactors worldwide from 1954
            to 2019" ;
    :xAxis :XAxis ;
    :yAxis :YAxis .

:XAxis a :Axis ; :title "Year" .
:YAxis a :Axis ;
    :title "Number of nuclear power plants" .

:DataPoint1 a :DataPoint ;
    :xValue "Dec 31, 1953" ;
    :yValue "1" ;
    :belongsTo :Chart .

...

:DataPoint66 a :DataPoint ;
    :xValue "Dec 31, 2018" ;
    :yValue "443" ;
    :belongsTo :Chart .
\end{lstlisting}
\end{minipage}
\caption{Representative VisText chart-to-RDF example. Upper left: the compact target schema. Lower left: a test-split area chart. Right: an excerpt of the corresponding Turtle graph. The full reference graph contains 66 datapoints, illustrating that chart-to-RDF extraction must recover both numeric values and the intended datapoint sequence.}
\label{fig:vistext_example}
\end{figure}

\subsubsection{Diagram2Graph-derived Diagram-to-RDF Dataset}
Diagram2Graph provides diagram images and JSON annotations describing nodes, edges, labels, shapes, and relationship types \citep{diagram2graph}. We use the dataset as a diagram-to-RDF benchmark by expressing the same graph information in Turtle. The target output contains one resource for each visible diagram node and one resource for each directed edge. Node resources encode the visible node label, node type, and shape. Edge resources encode source node, target node, edge style, relationship type, and optional visible edge label. The Diagram2Graph target schema and a representative diagram-to-RDF example are jointly shown in Figure~\ref{fig:diagram_example}.

\begin{figure}[H]
\centering
\begin{minipage}[ht]{0.52\textwidth}
\vspace{0pt}
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{tabularx}{\linewidth}{@{}lX@{}}
\toprule
\textbf{Component} & \textbf{Vocabulary / target} \\
\midrule
Nodes & \texttt{:Node}, \texttt{:Start}, \texttt{:Process}, \texttt{:Decision}, \texttt{:Delay}, \texttt{:Terminator} \\
Shapes & \texttt{:StartEvent}, \texttt{:EndEvent}, \texttt{:Task}, \texttt{:Gateway}, \texttt{:DataStore} \\
Edges & \texttt{:Edge}, \texttt{:Solid}, \texttt{:Dashed}, \texttt{:Follows}, \texttt{:Branches}, \texttt{:DependsOn} \\
Predicates & \texttt{:label}, \texttt{:shape}, \texttt{:source}, \texttt{:target}, \texttt{:relationshipType}, \texttt{:relationshipValue} \\
\bottomrule
\vspace{1pt}
\end{tabularx}
\centering
\includegraphics[width=0.8\linewidth]{img/diagram2graph_example.png}
\end{minipage}\hfill
\begin{minipage}[ht]{0.45\textwidth}
\vspace{0pt}
\begin{lstlisting}[style=turtlestyle]
@prefix : <http://example.org/diagram2graph#> .

:Node1 a :Start, :Node ;
    :label "Begin" ;
    :shape :StartEvent .

:Node5 a :Decision, :Node ;
    :label "Error<=Error" ;
    :shape :Gateway .

:Node6 a :Decision, :Node ;
    :label "Epoch>=Epoch" ;
    :shape :Gateway .

:Node9 a :Terminator, :Node ;
    :label "End" ;
    :shape :EndEvent .

:Edge5 a :Solid, :Edge ;
    :source :Node5 ; :target :Node9 ;
    :relationshipType :Follows ;
    :relationshipValue "Yes" .

:Edge6 a :Solid, :Edge ;
    :source :Node5 ; :target :Node6 ;
    :relationshipType :Follows ;
    :relationshipValue "No" .
\end{lstlisting}
\end{minipage}
\caption{Representative Diagram2Graph diagram-to-RDF example. Upper left: the compact target schema. Lower left: a labelled flow/process diagram. Right: an excerpt of the target Turtle graph showing node typing, visible labels, node shapes, directed edges, and labelled edge semantics.}
\label{fig:diagram_example}
\end{figure}

\subsection{Prompting Strategies}\label{subsec:prompting}

All generation strategies use a dataset-specific zero-shot system prompt that defines the task, schema, and output constraints. The complete zero-shot system prompts are provided in~\ref{app:system_prompts}.

\subsubsection{Zero-Shot Prompting}
The model receives only the system prompt, a short user instruction, and the target image. This setting tests whether the model can infer the required graph directly from the task description and schema.

\subsubsection{Static One-Shot Prompting}
The model receives one curated image-to-RDF example before the target image. For VisText, the static one-shot example is a bar chart because bar charts are common in the dataset. For Diagram2Graph, the one-shot example is a representative diagram.

\subsubsection{Dynamic One-Shot Prompting}
Dynamic one-shot prompting is used for VisText because the dataset contains multiple chart types. A preliminary classification step assigns the input chart to the bar, line, or area category. The extraction prompt then uses the example and chart-specific instructions corresponding to that predicted type. Diagram2Graph does not use dynamic one-shot prompting because all inputs are diagrams.

\subsubsection{Few-Shot Prompting}
Few-shot prompting provides all curated examples for the dataset. VisText uses one bar chart, one line chart, and one area chart. Diagram2Graph uses three diagram examples.

\subsection{Generation Model and Validity Criterion}

All image-to-RDF experiments use Gemini 3 Flash. The model is not fine-tuned on either labelled dataset. A generated output is considered eligible for evaluation only if it can be parsed as RDF/Turtle.

\section{Evaluation Results}\label{sec:evaluation}

\subsection{Traditional Graph Metrics for Labelled Datasets}\label{subsec:traditional_graph_metrics}

For VisText and Diagram2Graph, predicted RDF graphs are compared with reference RDF graphs after parsing and normalization. Following recent work on LLM-based KG construction evaluation \citep{ghanemcruz2025kgc}, we report a set of complementary metrics since different metrics measure different properties of graph quality.

The metric set is organized into four categories:

\begin{itemize}
    \item \textbf{Exact triple overlap.} Triple-match precision, recall, and F1 measure exact overlap between predicted and reference triples. This is the strictest content-oriented metric family and directly penalizes missing or hallucinated triples. Triple-match accuracy is also reported as a prediction-oriented exact-match rate, measuring the proportion of predicted triples that appear in the reference graph.
    \item \textbf{Graph edit distance.} Normalized graph edit distance (GED) treats each RDF graph as a labelled directed multigraph and estimates the normalized edit cost required to transform the predicted graph into the reference graph. This metric captures structural differences that may not be visible from aggregate triple counts alone.
    \item \textbf{Lexical triple similarity.} BLEU and ROUGE compare serialized triples as textual units under one-to-one assignment \citep{bleu,rouge}. These metrics provide softer lexical similarity signals when a predicted triple is close to a reference triple but not exactly identical.
    \item \textbf{Embedding-based triple similarity.} G-BERTScore applies contextual embedding similarity to serialized triples and is intended to capture semantically close but lexically non-identical triples \citep{bertscore}.
\end{itemize}

The formal definitions used in the paper are provided in~\ref{app:metric_definitions}.

\subsection{VisText Evaluation Modes and Numeric Tolerance}\label{subsec:vistext_eval_modes}

The VisText schema is designed to be semantically complete: its full RDF graph can be used directly as a chart knowledge graph because it includes chart type, axis resources, datapoint resources, and membership relations. For evaluation, however, this completeness can overestimate model performance because many triples are fixed by the schema once the model has adopted the expected output pattern. We therefore report both full-graph and content-only modes. Full-graph mode evaluates every normalized triple. Content-only mode retains literal-valued triples and the chart-type triple, while omitting fixed schema-completion triples. In Figure~\ref{fig:vistext_example}, for example, triples such as \texttt{:XAxis a :Axis}, \texttt{:DataPoint1 a :DataPoint}, and \texttt{:DataPoint1 :belongsTo :Chart} are omitted in content-only mode, whereas triples such as \texttt{:Chart :title "..."}, \texttt{:XAxis :title "Year"}, \texttt{:DataPoint1 :xValue "Dec 31, 1953"}, \texttt{:DataPoint1 :yValue "1"}, and \texttt{:Chart a :AreaChart} are retained.

VisText evaluation also includes an optional relative numeric tolerance for datapoint values. In each chart, one coordinate identifies the datapoint and the other coordinate gives the numeric value to be estimated from the visual mark. The tolerance is applied only to this numeric value. This is needed because the model receives only the chart image: for many charts, a visually reasonable estimate may be close to the reference value but not exactly identical. We report the tolerance sweep only for triple-match precision, recall, F1, triple-match accuracy, and normalized GED because these metrics depend on exact graph overlap. Without tolerance, a visually close estimate such as 0.38 for a reference value of 0.379 is counted as a non-match. BLEU, ROUGE, and G-BERTScore are not included in the tolerance sweep because they already measure lexical or embedding-based similarity.

\subsection{LLM-as-a-Judge}

Traditional graph metrics require a reference RDF graph and therefore cannot be
applied directly to unlabelled scientific image collections. This limitation is
central to the Soil-Health application considered in Section~\ref{sec:soil_use_case}:
the source images contain potentially useful knowledge, but no complete gold
RDF graphs are available. We therefore introduce an LLM-as-a-judge framework as
a complementary evaluation instrument for unlabelled image-to-RDF outputs.

The judge framework is adapted from KGEval, which evaluates knowledge graphs
through criterion-based LLM assessment \citep{kgeval}. Table~\ref{tab:judge_rubric}
summarizes the rubric used in the judge. We modify the KGEval design for the
image-to-RDF setting by grounding every judgment in the source image and the
candidate RDF/Turtle graph. The judge is instructed to assess whether the graph
captures visible image content, avoids unsupported triples, follows the provided
schema, and remains internally coherent as an RDF graph. This design is
image-type agnostic: the same judging framework can be applied to charts,
flowcharts, diagrams, and table images because the input consists of an image,
schema guidance, and one or more candidate RDF graphs.

\begin{table}[ht]
\centering
\small
\begin{tabularx}{\textwidth}{@{}lY@{}}
\toprule
\textbf{Criterion} & \textbf{Assessment focus} \\
\midrule
Relevance & Whether the graph focuses on visible, task-relevant image content \\
Factuality & Whether entities, labels, values, and relations are grounded in the image \\
Informativeness & Whether the graph captures the major useful content of the image \\
Coherence & Whether the RDF graph is internally consistent and structurally meaningful \\
Specificity & Whether triples are precise rather than vague or generic \\
\bottomrule
\end{tabularx}
\caption{LLM-as-a-judge rubric adapted from KGEval for image-to-RDF evaluation.}
\label{tab:judge_rubric}
\end{table}

The judge is used in two complementary modes. In direct scoring, the judge
receives the source image and one candidate RDF/Turtle graph. It assigns five
criterion scores and one overall score on a 1-to-5 scale. In pairwise
comparison, the judge receives the same source image and two candidate graphs
generated by different prompting strategies. It selects the better graph for
each criterion and gives an overall preference. Direct scoring supports
absolute quality analysis, while pairwise comparison supports strategy ranking
without requiring the judge to calibrate scores across unrelated images. The
system prompts used for the two judge modes are provided in~\ref{app:judge_prompts}.

We use OpenAI GPT-5-mini \citep{openai2025gpt5systemcard} as the judge model,
whereas image-to-RDF generation uses Gemini 3 Flash. Using a different model
family for judging reduces the risk that the evaluator systematically favours
generations from its own model family \citep{spiliopoulou2025playfavorites}. We first validate the judge on the two labelled datasets, where direct judge scores can be compared with traditional metric scores and pairwise judge preferences can be compared with metric-derived preferences. After this validation step, the same judge framework is applied to the unlabelled Soil-Health application.

\subsection{Evaluation Results}\label{sec:results}

\subsubsection{VisText Evaluation}

Table~\ref{tab:vistext_strategy_results} reports the main VisText evaluation results for all four prompting strategies. Each strategy is evaluated on the same 868 usable test-split images in both whole-graph and content-only modes, as defined in Section~\ref{subsec:vistext_eval_modes}. The reported precision, recall, F1, triple-match accuracy, and normalized GED use a 1\% numeric tolerance for datapoint values; ROUGE, BLEU, and G-BERTScore are not tolerance-adjusted. The metrics follow the definitions in Section~\ref{subsec:traditional_graph_metrics}.

Table~\ref{tab:vistext_chart_type_results} reports the content-only results by chart type. The chart-type subsets contain 417 bar charts, 213 line charts, and 238 area charts. Dynamic one-shot prompting includes an image classification step before RDF generation; this chart-type classifier achieved 100\% accuracy on the evaluated VisText test split.

Table~\ref{tab:vistext_tolerance_results} reports the numeric-tolerance sweep for zero-shot prompting in content-only mode. The tolerance setting follows Section~\ref{subsec:vistext_eval_modes} and is applied to the numeric datapoint value only.

\begin{table*}[ht]
\centering
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{llcccccccc}
\toprule
\textbf{Strategy} & \textbf{Mode} & \textbf{P} & \textbf{R} & \textbf{F1} & \textbf{Acc.} & \textbf{nGED} & \textbf{R-F1} & \textbf{B-F1} & \textbf{GBS-F1} \\
\midrule
Zero-shot & Whole graph & 0.8206 & 0.7522 & 0.7849 & 0.8715 & 0.0749 & 0.9560 & 0.9572 & 0.9710 \\
Static one-shot & Whole graph & 0.8241 & 0.7536 & 0.7873 & 0.8697 & 0.0738 & 0.9580 & 0.9597 & 0.9735 \\
Dynamic one-shot & Whole graph & 0.8373 & \textbf{0.7545} & \textbf{0.7938} & \textbf{0.8737} & \textbf{0.0718} & 0.9583 & 0.9602 & 0.9738 \\
Few-shot & Whole graph & \textbf{0.8389} & 0.7522 & 0.7932 & 0.8728 & 0.0722 & \textbf{0.9597} & \textbf{0.9619} & \textbf{0.9756} \\
\addlinespace
Zero-shot & Content only & 0.6979 & 0.6395 & 0.6674 & 0.7622 & 0.0961 & 0.9395 & 0.9418 & 0.9695 \\
Static one-shot & Content only & 0.6978 & 0.6378 & 0.6664 & 0.7569 & 0.0959 & 0.9409 & 0.9443 & 0.9720 \\
Dynamic one-shot & Content only & \textbf{0.7142} & \textbf{0.6432} & \textbf{0.6769} & \textbf{0.7633} & \textbf{0.0929} & 0.9414 & 0.9450 & 0.9723 \\
Few-shot & Content only & 0.7101 & 0.6364 & 0.6713 & 0.7599 & 0.0945 & \textbf{0.9423} & \textbf{0.9468} & \textbf{0.9741} \\
\bottomrule
\end{tabular}%
}
\caption{VisText evaluation by prompting strategy and graph mode. P, R, and F1 denote exact triple-match precision, recall, and F1; Acc. denotes mean triple-match accuracy; nGED denotes normalized GED; R-F1, B-F1, and GBS-F1 denote ROUGE, BLEU, and G-BERTScore F1. Bold values indicate the best result within each graph mode.}


\label{tab:vistext_strategy_results}
\end{table*}

\begin{table*}[ht]
\centering
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{llcccccccc}
\toprule
\textbf{Chart} & \textbf{Strategy} & \textbf{P} & \textbf{R} & \textbf{F1} & \textbf{Acc.} & \textbf{nGED} & \textbf{R-F1} & \textbf{B-F1} & \textbf{GBS-F1} \\
\midrule
Bar & Zero-shot & \textbf{0.7875} & \textbf{0.7860} & \textbf{0.7867} & \textbf{0.7943} & \textbf{0.0624} & \textbf{0.9728} & \textbf{0.9705} & \textbf{0.9964} \\
Bar & Static one-shot & 0.7790 & 0.7764 & 0.7777 & 0.7868 & 0.0657 & 0.9706 & 0.9697 & 0.9958 \\
Bar & Dynamic one-shot & 0.7785 & 0.7768 & 0.7776 & 0.7854 & 0.0642 & 0.9717 & 0.9699 & 0.9962 \\
Bar & Few-shot & 0.7807 & 0.7778 & 0.7793 & 0.7901 & 0.0640 & 0.9696 & 0.9697 & 0.9955 \\
\addlinespace
Line & Zero-shot & 0.6468 & 0.5911 & 0.6177 & 0.7473 & 0.1188 & 0.9185 & 0.9238 & 0.9520 \\
Line & Static one-shot & 0.6566 & 0.5981 & 0.6259 & 0.7481 & 0.1132 & 0.9224 & 0.9289 & 0.9574 \\
Line & Dynamic one-shot & \textbf{0.6902} & \textbf{0.6144} & \textbf{0.6501} & \textbf{0.7703} & \textbf{0.1058} & \textbf{0.9235} & \textbf{0.9316} & \textbf{0.9585} \\
Line & Few-shot & 0.6586 & 0.5898 & 0.6223 & 0.7458 & 0.1181 & 0.9230 & 0.9310 & 0.9584 \\
\addlinespace
Area & Zero-shot & 0.6175 & 0.5092 & 0.5581 & 0.7193 & 0.1346 & 0.9001 & 0.9075 & 0.9381 \\
Area & Static one-shot & 0.6203 & 0.5096 & 0.5596 & 0.7125 & 0.1333 & 0.9053 & 0.9137 & 0.9436 \\
Area & Dynamic one-shot & 0.6428 & \textbf{0.5125} & 0.5703 & 0.7185 & 0.1316 & 0.9043 & 0.9135 & 0.9430 \\
Area & Few-shot & \textbf{0.6528} & 0.5105 & \textbf{0.5729} & \textbf{0.7195} & \textbf{0.1269} & \textbf{0.9119} & \textbf{0.9210} & \textbf{0.9505} \\
\bottomrule
\end{tabular}%
}
\caption{VisText content-only evaluation by chart type. Abbreviations follow Table~\ref{tab:vistext_strategy_results}. Bold values indicate the best result within each chart type.}
\label{tab:vistext_chart_type_results}
\end{table*}

\begin{table*}[ht]
\centering
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
\textbf{Tolerance} & \textbf{P} & \textbf{R} & \textbf{F1} & \textbf{Acc.} & \textbf{nGED} & \textbf{Matched/Cand.} \\
\midrule
0\% & 0.5599 & 0.5131 & 0.5355 & 0.6103 & 0.1378 & 3{,}200 / 11{,}582 (27.6\%) \\
0.5\% & 0.6470 & 0.5928 & 0.6187 & 0.7076 & 0.1111 & 6{,}156 / 11{,}582 (53.2\%) \\
1\% & 0.6979 & 0.6395 & 0.6674 & 0.7622 & 0.0961 & 7{,}833 / 11{,}582 (67.6\%) \\
1.5\% & 0.7236 & 0.6631 & 0.6920 & 0.7892 & 0.0891 & 8{,}688 / 11{,}582 (75.0\%) \\
2\% & 0.7368 & 0.6752 & 0.7047 & 0.8028 & 0.0854 & 9{,}152 / 11{,}582 (79.0\%) \\
\bottomrule
\end{tabular}%
}
\caption{VisText zero-shot numeric tolerance analysis in content-only mode. Abbreviations follow Table~\ref{tab:vistext_strategy_results}. Matched/Cand. reports matched numeric value literals over all candidate numeric value literals.}
\label{tab:vistext_tolerance_results}
\end{table*}

\subsubsection{Diagram2Graph Evaluation}

Table~\ref{tab:diagram_results} reports the Diagram2Graph labelled evaluation for the three RDF prompting strategies. The evaluation uses 216 diagrams after excluding the three images used as in-context examples, so all strategies are compared on the same image set. Because the target graph represents diagram topology through nodes, edges, labels, source-target links, line styles, and relationship types, the full RDF graph is evaluated directly.

\begin{table*}[ht]
\centering
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccccc}
\toprule
\textbf{Strategy} & \textbf{P} & \textbf{R} & \textbf{F1} & \textbf{Acc.} & \textbf{nGED} & \textbf{R-F1} & \textbf{B-F1} & \textbf{GBS-F1} \\
\midrule
Zero-shot & \textbf{0.8723} & \textbf{0.8766} & \textbf{0.8744} & \textbf{0.8841} & \textbf{0.0624} & \textbf{0.9838} & \textbf{0.9891} & \textbf{0.9968} \\
One-shot & 0.8718 & 0.8761 & 0.8740 & 0.8835 & 0.0626 & 0.9833 & 0.9888 & 0.9968 \\
Few-shot & 0.8390 & 0.8597 & 0.8493 & 0.8600 & 0.0799 & 0.9682 & 0.9749 & 0.9847 \\
\bottomrule
\end{tabular}%
}
\caption{Diagram2Graph evaluation by prompting strategy on the same 216-image labelled evaluation set after excluding the three in-context example images. Abbreviations follow Table~\ref{tab:vistext_strategy_results}.}
\label{tab:diagram_results}
\end{table*}

\subsubsection{LLM-as-a-Judge Validation}

Tables~\ref{tab:judge_direct_validation} and \ref{tab:judge_pairwise_validation} report the LLM-as-a-judge validation results on the two labelled datasets. For Diagram2Graph, the validation used the two prompting strategies with the largest aggregate metric difference, few-shot and zero-shot, and selected the 57 diagrams for which either the triple-match F1 gap or the normalized GED gap exceeded 0.05. The 0.05 threshold was used because the judge operates with a coarse 1--5 rating scale; when two generated graphs are nearly identical under the traditional metrics, the judge is unlikely to assign a meaningful distinction between them. For VisText, the same selection procedure identified dynamic one-shot and static one-shot as the strategy pair for validation, resulting in 268 chart images. Direct scoring compares judge scores with traditional metric scores on the selected samples. Pairwise validation compares judge preferences with metric-derived preferences after anonymizing the two candidate graphs as Candidate A and Candidate B.

\begin{table}[ht]
\centering
\small
\begin{tabular}{llcccc}
\toprule
\textbf{Dataset} & \textbf{Strategy} & \textbf{Overall} & \textbf{Criteria} & \textbf{F1} & \textbf{nGED} \\
\midrule
Diagram2Graph & Zero-shot & \textbf{4.8158} & \textbf{4.8854} & \textbf{0.8509} & \textbf{0.0776} \\
Diagram2Graph & Few-shot & 3.6901 & 3.9234 & 0.7718 & 0.1393 \\
\addlinespace
VisText & Dynamic one-shot & \textbf{3.7836} & \textbf{4.0216} & \textbf{0.7126} & \textbf{0.0957} \\
VisText & Static one-shot & 3.7799 & 4.0060 & 0.6973 & 0.1059 \\
\bottomrule
\end{tabular}
\caption{Direct LLM-as-a-judge validation on selected labelled-dataset samples. Overall denotes the holistic judge score; Criteria denotes the mean criterion score; F1 denotes triple-match F1; nGED denotes normalized GED.}
\label{tab:judge_direct_validation}
\end{table}

\begin{table}[ht]
\centering
\small
\begin{tabular}{lccc}
\toprule
\textbf{Dataset} & \textbf{Compared strategies} & \textbf{Agreement} & \textbf{N} \\
\midrule
Diagram2Graph & Few-shot vs zero-shot & 0.4737 & 57 \\
VisText & Dynamic one-shot vs static one-shot & 0.4963 & 268 \\
\bottomrule
\end{tabular}
\caption{Pairwise LLM-as-a-judge validation against metric-derived preferences.}
\label{tab:judge_pairwise_validation}
\end{table}

\section{Discussion}\label{sec:discussion}

\subsection{VisText: Chart Extraction and Numeric Estimation}

The VisText results show that direct chart-to-RDF generation is feasible, but that the main difficulty lies in recovering content-bearing triples instead of producing the fixed ontology pattern. Whole-graph F1 scores range from 0.7849 to 0.7938, whereas content-only F1 scores range from 0.6664 to 0.6769. This difference is expected because whole-graph evaluation includes schema-completion triples such as axis declarations and datapoint membership relations. Content-only evaluation is therefore more informative for comparing how well the model recovers chart type, textual chart elements, and datapoint values from the image.

\textbf{Prompting Strategy Effects.} Dynamic one-shot prompting gives the strongest exact graph-overlap performance on the full VisText test split. In content-only mode, it obtains the best triple precision, recall, F1, triple-match accuracy, and normalized GED among the four prompting strategies. The gain over zero-shot prompting is consistent but modest: content-only F1 increases from 0.6674 to 0.6769, and normalized GED decreases from 0.0961 to 0.0929. This suggests that chart-type-specific prompting helps align the output with the expected RDF representation, but it does not remove the main visual-estimation challenges.

Few-shot prompting shows a different profile. It produces the highest ROUGE-F1, BLEU-F1, and G-BERTScore-F1 in content-only mode, and the highest whole-graph precision and soft similarity scores. However, it does not achieve the best exact triple-recovery F1. This indicates that in-context examples can improve lexical and semantic resemblance to the reference graphs while still leaving exact triple recovery constrained by visual estimation, datapoint alignment, and value extraction.

\textbf{Chart-Type Differences.} The chart-type breakdown shows that visual form has a larger effect on performance than the choice among the tested prompting strategies. Bar charts obtain the strongest results, with the best content-only F1 reaching 0.7867 and the best normalized GED reaching 0.0624. This is consistent with their visual structure: bar charts usually present a finite set of separated marks whose category labels and values can be paired more directly.

Line and area charts are substantially more difficult. The best line-chart F1 is 0.6501, and the best area-chart F1 is 0.5729. These chart types often require the model to infer the intended datapoint sequence from the title, axes, tick marks, and plotted geometry, and further estimate values of datapoints from continuous marks. The lower area-chart scores indicate that filled regions and dense temporal sequences remain particularly challenging for direct image-to-RDF extraction.

\textbf{Numeric Tolerance and Value Estimation.} The numeric-tolerance analysis in Table~\ref{tab:vistext_tolerance_results} indicates that many visually inferred datapoint values are very close to the reference values even when they are not exact string matches. Under zero-shot prompting, 27.6\% of eligible numeric datapoint literals match exactly. Introducing only a 0.5\% relative tolerance increases the matched-literal ratio to 53.2\%, nearly doubling the exact-match result. At 1\% tolerance, 67.6\% of eligible numeric literals are matched, and at 2\% tolerance the matched-literal ratio reaches 79.0\%.

This result is important because the model predicts these values directly from the chart image, without access to the underlying data table. The tolerance sweep therefore suggests that many numerical errors are small visual-estimation deviations. An extended sweep to 3\%, 4\%, and 5\% further raises the matched-literal ratio to 83.9\%, 86.8\%, and 88.7\%, respectively, but the largest gain already occurs within the first 2\% tolerance interval. We therefore interpret the 0--2\% range as the most informative region for characterizing typical VLM value-estimation accuracy in this task: most recoverable numeric datapoint predictions are close to the reference values, even under a strict image-only extraction setting.

\subsubsection{VisText Error Patterns}

The most severe VisText failures occur when the model infers an incorrect
sampling resolution for line and area charts. Image \texttt{3975} provides a
representative case. The chart title states that the figure shows the stock
price of Unilever at Euronext Amsterdam from August 2019 to January 2021. In
the zero-shot output, the model interprets this temporal range as a monthly
series and produces one datapoint for each month, with values such as
\texttt{"August 2019"} and \texttt{"September 2019"} as the x-values. The
reference RDF graph, however, represents the plotted series at daily-date
resolution, beginning with dates such as \texttt{"Jul 31, 2019"} and
\texttt{"Aug 01, 2019"}. Consequently, the predicted graph contains 18
datapoints, whereas the reference graph contains 369 datapoints.

This mismatch explains why the metric scores for this chart are close to zero:
the model does not merely estimate slightly inaccurate numeric values, but
constructs the graph at the wrong temporal granularity. The example illustrates
a limitation of image-only chart-to-RDF extraction. When the visual marks are
dense and individual datapoints are not explicitly labelled, the model must
infer both the number of datapoints and their values from the image and its
semantic context. In such cases, a plausible but incorrect assumption about the
sampling interval can dominate the evaluation outcome. This failure mode should
therefore be interpreted primarily as an information-availability limitation:
without access to the underlying data table or explicit datapoint labels, even a
semantically reasonable reading of the chart can produce a graph that is
structurally incompatible with the reference RDF.


\subsection{Diagram2Graph: Diagram Structure Extraction}

Diagram2Graph evaluates a different form of image-to-RDF construction from VisText. Instead of estimating numerical values from plotted marks, the model must recover diagram topology: node identities, node classes, edge direction, line style, and relationship type. Errors in this setting can directly change the represented process or dependency structure.

The results in Table~\ref{tab:diagram_results} show that zero-shot prompting with an explicit schema is sufficient for strong Diagram2Graph performance. Zero-shot prompting obtains the best value for every reported metric, with triple-match F1 of 0.8744, triple-match accuracy of 0.8841, and normalized GED of 0.0624. Static one-shot prompting is almost identical to zero-shot prompting, whereas few-shot prompting gives lower exact triple-match scores and a higher normalized GED. This pattern suggests that, for this compact diagram schema, adding examples does not necessarily improve extraction. The schema itself already specifies the required node and edge vocabulary, and additional examples may introduce local visual or topological patterns that are not representative of the broader diagram set.

\subsubsection{JSON-versus-RDF Serialization Ablation}

The JSON-versus-RDF ablation in Table~\ref{tab:json_rdf_ablation} tests whether requesting RDF/Turtle output causes a performance decline relative to a JSON serialization of the same diagram information. JSON and RDF/Turtle encode the same node-edge graph in this experiment, and the two serializations can be converted into each other without information loss. Their evaluation scores are therefore comparable under the same graph metrics. This ablation is necessary because, if JSON output were better than RDF/Turtle output, a JSON-first pipeline followed by deterministic RDF conversion would be preferable to direct RDF/Turtle generation. The comparison uses the same 20-image subset for all rows. The JSON baseline is a Qwen2.5-VL-3B model \citep{bai2025qwen25vl} fine-tuned with LoRA \citep{hu2022lora} on 199 Diagram2Graph training examples; its reported scores are the mean of two inference runs. The Gemini rows are single-run prompting results.

\begin{table*}[ht]
\centering
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{llcccccccc}
\toprule
\textbf{System} & \textbf{Format} & \textbf{P} & \textbf{R} & \textbf{F1} & \textbf{Acc.} & \textbf{nGED} & \textbf{R-F1} & \textbf{B-F1} & \textbf{GBS-F1} \\
\midrule
Gemini zero-shot & JSON & 0.7552 & 0.7552 & 0.7552 & 0.7773 & 0.0931 & 0.9692 & 0.9811 & 0.9982 \\
Gemini one-shot & JSON & 0.8150 & 0.8150 & 0.8150 & 0.8381 & 0.0690 & 0.9771 & 0.9868 & 0.9988 \\
Gemini few-shot & JSON & 0.8256 & 0.8214 & 0.8235 & 0.8505 & 0.0651 & 0.9758 & 0.9846 & 0.9962 \\
Qwen2.5-VL-3B (2-run mean) & JSON & 0.7349 & 0.7171 & 0.7259 & 0.7712 & 0.1311 & 0.9358 & 0.9518 & 0.9691 \\
Gemini RDF zero-shot & RDF/Turtle & \textbf{0.8599} & \textbf{0.8599} & \textbf{0.8599} & \textbf{0.8801} & \textbf{0.0643} & \textbf{0.9843} & \textbf{0.9906} & \textbf{0.9993} \\
Gemini RDF one-shot & RDF/Turtle & 0.8382 & 0.8382 & 0.8382 & 0.8594 & 0.0729 & 0.9811 & 0.9883 & 0.9990 \\
Gemini RDF few-shot & RDF/Turtle & 0.8249 & 0.8313 & 0.8281 & 0.8518 & 0.0866 & 0.9692 & 0.9779 & 0.9904 \\
\bottomrule
\end{tabular}%
}
\caption{Diagram2Graph JSON-versus-RDF serialization ablation on the same 20-image subset. Abbreviations follow Table~\ref{tab:vistext_strategy_results}. The Qwen baseline reports the mean over two inference runs.}
\label{tab:json_rdf_ablation}
\end{table*}

The ablation results do not show a decline when the target output is RDF/Turtle. On the same subset, the RDF/Turtle prompting results do not indicate a performance penalty relative to JSON serialization. This supports the use of direct RDF/Turtle generation in the proposed image-to-KG task. Furthermore, throughout the experiments conducted in this paper, the LLM produced only one instance of RDF/Turtle with syntax error, demonstrating that the frontier model is currently capable of robustly generating RDF.

The table also shows that all Gemini prompting strategies outperform the LoRA-fine-tuned Qwen2.5-VL-3B baseline on the reported metrics. In this setting, a frontier multimodal model prompted with the target schema provides stronger structured extraction than a smaller model adapted to the task with parameter-efficient fine-tuning. This result indicates that prompt-based frontier models can be highly competitive for diagram-to-graph extraction even without task-specific parameter updates.

\subsection{Cross-Dataset Implications}

Across the two labelled datasets, the results support the feasibility of direct
image-to-RDF construction from heterogeneous infographics. VisText and
Diagram2Graph differ substantially in visual form and target semantics: VisText
requires recovering chart type, textual elements, and datapoint values from
charts, whereas Diagram2Graph requires reconstructing node-edge topology from
diagrams. Nevertheless, both datasets obtain strong graph-similarity results
under direct RDF/Turtle generation. This indicates that with a clear schema, a frontier vision-language model can
produce RDF graphs directly from visual input.

A second common pattern is that prompt-engineering variants have limited
effects compared with the information contained in the schema and the image
itself. For both labelled datasets, zero-shot prompting with explicit schema
guidance is already competitive. On VisText, dynamic one-shot prompting gives
the best exact graph-overlap scores, but the gain over zero-shot prompting is
small. On Diagram2Graph, zero-shot prompting is the best-performing strategy
across the reported metrics, and few-shot prompting is worse than zero-shot.
These results suggest that, for recent multimodal models such as Gemini 3
Flash, providing a precise task definition and RDF schema is more important than
adding more in-context examples. Additional examples can help when they clarify
the intended schema, but they can also introduce local visual or structural
patterns that do not generalize to the broader dataset.

The main performance bottleneck is therefore the amount and clarity of information available in the image. Diagram images
often contain explicit symbolic elements: nodes, arrows, labels, and line
styles. By contrast, line and area charts may require the model to infer an
unlabelled datapoint sequence and estimate numerical values from dense visual
marks. This explains why Diagram2Graph reaches higher exact graph scores than
VisText content-only evaluation, and why the most severe VisText errors are
associated with incorrect assumptions about datapoint granularity. The
cross-dataset evidence therefore points to a practical boundary for direct
image-to-RDF extraction: the approach is most reliable when the ontology is
explicit and the relevant visual evidence is directly observable, while
implicit or underspecified data resolution remains difficult without additional
source information.


\subsection{LLM-as-a-Judge Reliability}\label{sec:judge_reliability}

The labelled-dataset validation indicates that the proposed LLM-as-a-judge
framework is useful as a coarse quality assessor, but should not be treated as a
direct substitute for gold-standard graph metrics. Diagram2Graph provides the
clearer positive validation case. In direct scoring, the judge assigns a higher overall score to zero-shot outputs than to few-shot outputs
(4.8158 versus 3.6901), matching the traditional graph metrics, where zero-shot
outputs also have higher triple-match F1 and lower normalized GED on the
selected diagrams. This suggests that the judge can recognize substantial
differences in diagram-to-RDF quality when the competing outputs differ
visibly in graph completeness and structural correctness.

The pairwise validation results are more limited. For Diagram2Graph, agreement
between judge preferences and metric-derived preferences is 0.4737 on the
selected difficult diagrams. For VisText, the corresponding agreement is
0.4963, close to a balanced split. The VisText direct scores also differ only
slightly between the two compared strategies, consistent with their small
metric differences. These results indicate that the judge is less reliable for
fine-grained ranking between strong prompting strategies whose outputs are
close in quality. This limitation is expected for chart extraction, where many
differences involve visually ambiguous numeric estimates or datapoint
alignment, and where traditional metrics can penalize small literal-level
differences that a judge may consider semantically acceptable.

Accordingly, we use the judge framework for the unlabelled Soil-Health
application as a structured quality assessment. The validation supports its use for identifying broad
quality patterns and comparing clearly different outputs, while the labelled
metrics remain the primary evidence whenever reference RDF graphs are
available.


\section{Application: Soil-Health Image-to-RDF Extraction}\label{sec:soil_use_case}

The Soil-Health collection is used as an application case for image-to-RDF extraction on an unlabelled scientific corpus. The source material is the European Environment Agency report \textit{Soil monitoring in Europe: Indicators and thresholds for soil health assessments}, which summarizes indicators, threshold values, monitoring concepts, and assessment criteria for soil health in Europe \citep{eea2023soilmonitoring}. The report is also a central source for our previous soil health knowledge graph, which was constructed as an ontology-compliant KG with LLM support \citep{wang2025makesoilhealthy}. That first version focused mainly on textual content from the report and related sources, using a SKOS-based soil-health ontology.

The application considered here addresses a limitation of text-only KG construction. In the soil monitoring report, several important forms of knowledge are not fully expressed in the surrounding prose. Complete indicator lists, assessment thresholds, critical ranges, tabulated criteria, and diagrammatic summaries are often presented as figures or tables. If these visual artifacts are omitted during document preprocessing, the resulting KG can miss information that is directly relevant to soil-health assessment. The present application therefore evaluates whether the proposed image-to-RDF workflow can provide candidate RDF graphs that may support a future visually enriched version of the soil health KG.

The dataset contains 71 images from the report, including 15 figures and 56 tables. The target output is an RDF/Turtle graph using the same SKOS-based ontology as the soil health KG. For tables, the intended graph represents headers, grouped headers, row labels, cell values, legends, and semantically meaningful notes. For figures, it represents visible concepts, headings, diagram regions, arrows, captions, legends, and explicitly indicated associations. Since no complete gold RDF graphs are available for this collection, we apply the LLM-as-a-judge framework after validating it on the two labelled datasets. The same prompting regimes are considered as in the preceding experiments: zero-shot, static one-shot, dynamic one-shot, and few-shot. In the dynamic one-shot setting, the model first classifies the input image as a figure or table and then uses the corresponding in-context example. The figure and table images used as in-context examples are excluded from the reported application results.

Table~\ref{tab:soil_judge_results} reports direct judge scores for the four prompting strategies. The scores are high across all strategies, with mean overall scores between 4.7391 and 4.8261 on the 1-to-5 scale. Static one-shot prompting obtains the highest overall score and the highest scores for relevance, factuality, informativeness, and specificity. Dynamic one-shot prompting obtains the highest coherence score. The differences among strategies are small, indicating that all prompting regimes produce outputs judged to be broadly faithful and useful for this application. Together with the labelled-dataset results, this pattern further supports the observation that, for frontier multimodal models operating with explicit schema guidance, the marginal benefit of additional prompt-engineering variants can be limited.

\begin{table}[ht]
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lcccccc}
\toprule
\textbf{Strategy} & \textbf{Rel.} & \textbf{Fact.} & \textbf{Info.} & \textbf{Coh.} & \textbf{Spec.} & \textbf{Overall} \\
\midrule
Zero-shot & 4.9565 & 4.7826 & 4.7536 & 4.8551 & 4.8116 & 4.7971 \\
Static one-shot & \textbf{5.0000} & \textbf{4.8841} & \textbf{4.8261} & 4.8986 & \textbf{4.8696} & \textbf{4.8261} \\
Dynamic one-shot & 4.9855 & 4.8261 & 4.7971 & \textbf{4.9130} & 4.8116 & 4.7536 \\
Few-shot & 4.9420 & 4.8116 & 4.7246 & 4.8551 & 4.8406 & 4.7391 \\
\bottomrule
\end{tabular}
\caption{Soil-Health LLM-as-a-judge direct scoring results over 69 images per prompting strategy, excluding the two in-context example images. Rel., Fact., Info., Coh., and Spec. denote relevance, factuality, informativeness, coherence, and specificity; scores use a 1-to-5 rubric.}
\label{tab:soil_judge_results}
\end{table}

% To further compare the two strategies with the largest direct-score contrast, the judge was also applied in pairwise mode to the static one-shot and few-shot outputs. Table~\ref{tab:soil_pairwise_judge_results} shows that the pairwise preference is nearly balanced at the overall level, although the criterion-level preferences differ across evaluation dimensions.

% \begin{table}[ht]
% \centering
% \small
% \setlength{\tabcolsep}{6pt}
% \begin{tabular}{lccc}
% \toprule
% \textbf{Judgment} & \textbf{Few-shot} & \textbf{Static one-shot} & \textbf{Tie} \\
% \midrule
% Overall winner & 34 & 33 & 2 \\
% Relevance & 30 & 32 & 7 \\
% Factuality & 22 & 27 & 20 \\
% Informativeness & 33 & 33 & 3 \\
% Coherence & 24 & 38 & 7 \\
% Specificity & 36 & 30 & 3 \\
% \bottomrule
% \end{tabular}
% \caption{Soil-Health pairwise LLM-as-a-judge preferences between static one-shot and few-shot prompting over 69 images, excluding the two in-context example images. Values are counts of images for which each option was preferred.}
% \label{tab:soil_pairwise_judge_results}
% \end{table}

% The pairwise results refine the direct-score interpretation. Although static one-shot receives the highest mean direct score, the image-level pairwise comparison is nearly evenly split: few-shot is preferred for 34 images, static one-shot for 33 images, and 2 images are judged as ties. Criterion-level preferences also differ by dimension. Static one-shot is preferred more often for factuality and coherence, whereas few-shot is preferred more often for specificity. This pattern indicates that prompt examples affect different aspects of the generated graphs, but that no strategy dominates uniformly on the unlabelled Soil-Health collection. It also reinforces the broader experimental finding that prompt-strategy differences are modest for the frontier multimodal model used in this study. 

For KG integration, these outputs will therefore be treated as high-quality candidate graphs. The practical implication is that image-to-RDF extraction can extend the text-derived soil health KG with visually encoded indicator and threshold information, while domain review remains necessary before publication in a production KG.

\section{Conclusion}\label{sec:conclusion}

% This paper presents a prompt-based framework for constructing RDF knowledge graphs directly from scientific images. The approach uses Gemini 3 Flash for image-to-RDF generation and compares zero-shot, one-shot, dynamic one-shot, and few-shot prompting where applicable. It also introduces two labelled image-to-RDF resources derived from VisText and Diagram2Graph and an LLM-as-a-judge framework for unlabelled image collections. The labelled datasets support traditional graph-based evaluation, while the judge framework provides a validation-based route to evaluating unlabelled scientific image collections. The final experimental results will determine which prompting strategies are most reliable across image families and how closely judge-based assessment tracks traditional graph metrics.

\section*{Acknowledgments}
This work was supported by the EU’s Horizon Europe research and innovation programme within the SoilWise project (grant agreement ID: 101112838).

\appendix

\section{Zero-Shot System Prompts}\label{app:system_prompts}

\subsection{VisText Zero-Shot System Prompt}
\begin{lstlisting}[style=markdownstyle]
# Role
You are a multimodal knowledge graph construction system.
# Task
Read one chart image and output only valid RDF/Turtle encoding the chart's semantic content.
## Output
- RDF/Turtle only; no explanations, Markdown fences, comments, JSON, XML, YAML, or prose.
- Use exactly:
```turtle
@prefix : <http://example.org/vistext#> .
```
## Chart Types
- `:BarChart`, `:LineChart`, `:AreaChart`
## Schema
- Classes: `:BarChart`, `:LineChart`, `:AreaChart`, `:Axis`, `:DataPoint`
- Properties: `:title`, `:xAxis`, `:yAxis`, `:xValue`, `:yValue`, `:belongsTo`
## Required Structure
```turtle
:Chart a :BarChart / :LineChart / :AreaChart ;
    :title "..." ;
    :xAxis :XAxis ;
    :yAxis :YAxis .
:XAxis a :Axis ;
    :title "..." .
:YAxis a :Axis ;
    :title "..." .
:DataPoint1 a :DataPoint ;
    :xValue "..." ;
    :yValue "..." ;
    :belongsTo :Chart .
```
## Extraction Rules
- Extract the chart title exactly as shown.
- Extract visible x-axis and y-axis titles exactly as shown; omit missing axis-title triples rather than inventing them.
- For vertical bar charts, `xValue` is the category and `yValue` is the numeric bar value.
- For horizontal bar charts, `xValue` is the numeric bar value and `yValue` is the category.
- For line and area charts, `xValue` is the x-axis coordinate/category/time value and `yValue` is the numeric value.
- Infer the intended number of datapoints from chart semantics, especially title, axis labels, visible ticks, temporal range, and categorical range.
- For temporal annual line/area charts, normalize annual observations as `"Dec 31, YYYY"`.
- Represent all values as quoted strings without datatype annotations.
- Prefer exact values when readable; otherwise estimate values from axis ticks and geometry without inventing unsupported facts.
\end{lstlisting}

\subsection{Diagram2Graph Zero-Shot System Prompt}
\begin{lstlisting}[style=markdownstyle]
# Role
You are a diagram-to-graph extractor.
# Task
Given one flowchart or diagram image, output only valid RDF/Turtle using the lightweight diagram2graph vocabulary.
## Output
- RDF/Turtle only; no explanations, headings, comments, Markdown fences, JSON, XML, YAML, or extra text.
- Use exactly one prefix:
```turtle
@prefix : <http://example.org/diagram2graph#> .
```
## Ontology
- Node classes: `:Node`, `:Start`, `:Process`, `:Decision`, `:Delay`, `:Terminator`
- Shape values: `:StartEvent`, `:EndEvent`, `:Task`, `:Gateway`, `:DataStore`
- Edge classes: `:Edge`, `:Solid`, `:Dashed`
- Relationship values: `:Follows`, `:Branches`, `:DependsOn`
- Node properties: `:label`, `:shape`
- Edge properties: `:source`, `:target`, `:relationshipType`, `:relationshipValue`
## Resource Naming
- Name nodes as `:NodeN` and edges as `:EdgeN`.
- Number nodes and edges in visual reading order when explicit identifiers are absent.
## Node Triples
```turtle
:NodeN a :NodeType, :Node ;
    :label "exact visible text from the shape" ;
    :shape :ShapeValue .
```
## Edge Triples
```turtle
:EdgeN a :LineStyle, :Edge ;
    :source :NodeS ;
    :target :NodeT ;
    :relationshipType :RelationshipType .
```
If the arrow has a visible label, add:
```turtle
:relationshipValue "exact visible arrow label" .
```
## Constraints
- Do not emit direct node-to-node relationship triples.
- Do not use `rdfs:label`; use `:label`.
- Preserve visible text exactly.
- Every edge source and target must reference an emitted node resource.
- Every Turtle statement must end with a period.
\end{lstlisting}

\subsection{Soil-Health Zero-Shot System Prompt}
\begin{lstlisting}[style=markdownstyle]
# Role
You are a multimodal knowledge-extraction agent for soil-health figures and tables.
# Task
Read one soil-health image and output only valid RDF/Turtle triples that encode the concepts and relationships explicitly visible in the image.
## Output
- RDF/Turtle only; no explanations, headings, comments, Markdown fences, JSON, XML, or YAML.
- Emit these two prefixes first and use only these prefixes:
```turtle
@prefix she: <https://soilwise-he.github.io/soil-health#> .
@prefix skos: <http://www.w3.org/2004/02/skos/core#> .
```
## Concept Nodes
- Create a `she:` PascalCase URI for each visible concept and declare it as `skos:Concept`.
- Add `skos:prefLabel` using the visible label text in lowercase when it is a normal phrase.
- Preserve meaningful symbols and abbreviations through `skos:altLabel`, `she:hasAbbreviation`, or `she:hasSymbol` when visible.
- Use `skos:definition` or `skos:note` for visible definitions, notes, legends, or footnotes when they add semantic meaning.
- Deduplicate repeated labels.
## Relationships
- Use `skos:narrower` or `skos:broader` for containment, grouping, table header hierarchy, bullets, tree levels, and parent-child boxes.
- Use `skos:related` for visible associations where no direction or stronger relation is clear.
- For explicit non-hierarchical semantics, create a clear `she:` camelCase property such as `she:affects`, `she:measures`, `she:hasIndicator`, `she:isBasedOn`, `she:hasPositiveImpactOn`, `she:hasNegativeImpactOn`, or `she:hasNeutralImpactOn`.
- Respect arrow direction and labelled connector direction when visible.
- Do not invent concepts or relations not visible or directly implied by layout, legend, or labels.
## Tables
- Extract row headers, column headers, grouped headers, legend labels, and semantically meaningful cell values.
- For impact matrices, map positive impact or `+` to `she:hasPositiveImpactOn`, negative impact or `-` to `she:hasNegativeImpactOn`, and neutral/unknown/indifferent/indiff. to `she:hasNeutralImpactOn`.
## Figures
- Extract boxes, headings, lists, diagram regions, arrows, captions, legends, and notes when they carry semantic content.
- Model group boxes and regions with `skos:narrower`.
- Model arrows or connectors with the clearest `she:` camelCase property.
## Final Checklist
- The output starts with exactly the `she:` and `skos:` prefixes.
- Every concept URI is in the `she:` namespace and has `skos:Concept` plus `skos:prefLabel`.
- Turtle syntax is valid.
\end{lstlisting}

\section{Mathematical Definitions of Evaluation Metrics}\label{app:metric_definitions}

This appendix formalizes the graph metrics used in our evaluation implementation. The metric family follows prior LLM-based KG construction evaluation work \citep{ghanemcruz2025kgc}, while the definitions below state the exact variants used in this paper.

Let $\mathcal{I}=\{1,\ldots,N\}$ be the evaluated image set. For image $i$, let $G_i$ be the set of reference triples and $P_i$ the set of predicted triples after RDF parsing, graph projection, and normalization. A triple is written as $t=(s,p,o)$, and normalization lowercases and strips whitespace from all three terms. When a denominator is zero, the corresponding score is defined as zero.

\subsection{Exact Triple-Match Precision, Recall, and F1}
Exact triple matching treats $G_i$ and $P_i$ as sets. The micro-averaged true positives, false positives, and false negatives are
\[
TP=\sum_{i\in\mathcal{I}} |G_i\cap P_i|,\quad
FP=\sum_{i\in\mathcal{I}} |P_i\setminus G_i|,\quad
FN=\sum_{i\in\mathcal{I}} |G_i\setminus P_i|.
\]
The reported exact triple-match precision, recall, and F1 are
\[
\mathrm{P}_{T}=\frac{TP}{TP+FP},\quad
\mathrm{R}_{T}=\frac{TP}{TP+FN},\quad
\mathrm{F1}_{T}=\frac{2\mathrm{P}_{T}\mathrm{R}_{T}}{\mathrm{P}_{T}+\mathrm{R}_{T}}.
\]

\subsection{Triple-Match Accuracy}
Triple-match accuracy is computed per image as the fraction of predicted triples that are present in the reference graph:
\[
\mathrm{Acc}_{T,i}=\frac{|P_i\cap G_i|}{|P_i|}.
\]
The reported value is the arithmetic mean
\[
\overline{\mathrm{Acc}}_{T}=\frac{1}{N}\sum_{i\in\mathcal{I}}\mathrm{Acc}_{T,i}.
\]
This metric is prediction-oriented: it rewards predicted triples that exactly match the reference graph and penalizes hallucinated predicted triples, but it does not directly penalize omitted reference triples except through the denominator of other metrics.

\subsection{Normalized Graph Edit Distance}
Each triple set $S$ is converted into a labelled directed multigraph $\Phi(S)=(V_S,E_S)$:
\[
V_S=\{s\mid (s,p,o)\in S\}\cup \{o\mid (s,p,o)\in S\},
\]
\[
E_S=\{(s,o,p)\mid (s,p,o)\in S\},
\]
where node labels are the normalized RDF terms and edge labels are predicates. Let
\[
d_i=\mathrm{GED}\bigl(\Phi(G_i),\Phi(P_i)\bigr)
\]
denote the graph edit distance under exact node-label and edge-label matching. In implementation, this value is estimated with NetworkX iterative graph-edit optimization. The normalized graph edit distance for image $i$ is
\[
\mathrm{nGED}_i=
\frac{d_i}
{|V_{G_i}|+|E_{G_i}|+|V_{P_i}|+|E_{P_i}|},
\]
and the reported score is
\[
\overline{\mathrm{nGED}}=\frac{1}{N}\sum_{i\in\mathcal{I}}\mathrm{nGED}_i.
\]
Lower values indicate smaller structural distance between predicted and reference graphs.

\subsection{Assignment-Based Textual Graph Similarity}
BLEU, ROUGE, and G-BERTScore are computed over serialized triples. Let $\sigma(t)$ denote the textual serialization of triple $t$. For a similarity function $m$, define the pairwise score matrix for image $i$ as
\[
M^{(m)}_{ab}=m\bigl(\sigma(p_{ia}),\sigma(g_{ib})\bigr),
\quad p_{ia}\in P_i,\ g_{ib}\in G_i .
\]
A one-to-one alignment between predicted and reference triples is selected by maximum-weight bipartite matching:
\[
A_i^{(m)}=\arg\max_{A}\sum_{(a,b)\in A}M^{(m)}_{ab}.
\]
The per-image precision, recall, and F1 for metric $m$ are then
\[
\mathrm{P}^{(m)}_i=
\frac{\sum_{(a,b)\in A_i^{(m)}}M^{(m)}_{ab}}{|P_i|},\quad
\mathrm{R}^{(m)}_i=
\frac{\sum_{(a,b)\in A_i^{(m)}}M^{(m)}_{ab}}{|G_i|},
\]
\[
\mathrm{F1}^{(m)}_i=
\frac{2\mathrm{P}^{(m)}_i\mathrm{R}^{(m)}_i}
{\mathrm{P}^{(m)}_i+\mathrm{R}^{(m)}_i}.
\]
The paper reports arithmetic means over images for $\mathrm{P}^{(m)}$, $\mathrm{R}^{(m)}$, and $\mathrm{F1}^{(m)}$.

\subsection{BLEU}
For BLEU, $m_{\mathrm{BLEU}}(\sigma(p),\sigma(g))$ is sentence-level BLEU with the predicted serialized triple as candidate and the reference serialized triple as the single reference \citep{bleu}. The implementation uses NLTK sentence BLEU with smoothing method 1.

\subsection{ROUGE}
For ROUGE, $m_{\mathrm{ROUGE}}(\sigma(p),\sigma(g))$ is ROUGE-2 precision between the predicted serialized triple and the reference serialized triple \citep{rouge}. This pairwise ROUGE-2 precision is used as the assignment similarity; the reported ROUGE precision, recall, and F1 are then obtained from the assignment equations above.

\subsection{G-BERTScore}
For G-BERTScore, $m_{\mathrm{GBS}}(\sigma(p),\sigma(g))$ is BERTScore F1 between the predicted and reference serialized triples \citep{bertscore}. The implementation computes pairwise BERTScore without inverse-document-frequency weighting and applies the same maximum-weight assignment procedure used for BLEU and ROUGE.

\section{LLM-as-a-Judge Prompts}\label{app:judge_prompts}

\subsection{Direct Judge Prompt}
\begin{lstlisting}[style=markdownstyle]
# Role
You are an evaluator assessing an RDF/Turtle knowledge graph generated from an image. Judge whether the graph reflects the visible source image and is well constructed as a knowledge graph.
# Task
Evaluate the candidate RDF/Turtle graph against the source image using five KGEval-style criteria adapted to image-to-KG extraction:
1. Relevance: Does the graph focus on information that is visible and important in the image?
2. Factuality: Are entities, labels, values, relationships, and chart or table claims grounded in the image without hallucination?
3. Informativeness: Does the graph capture the major useful content, such as chart titles, axes, data points, table rows, columns, diagram nodes, and relationships?
4. Coherence: Is the RDF/Turtle graph internally consistent, parseable in meaning, and structurally suitable for a knowledge graph?
5. Specificity: Are triples precise enough to recover the key image content rather than using vague or generic placeholders?
Also assign an `overall_score` from 1 to 5 as a holistic quality score.
## Rating Scale
- `1`: unusable, unrelated, hallucinated, or incoherent.
- `2`: limited correct content with major omissions, hallucinations, or graph problems.
- `3`: partially correct but incomplete, noisy, or loosely aligned.
- `4`: mostly faithful and useful with minor omissions or local errors.
- `5`: faithful, informative, coherent, specific, and well aligned with the image.
## Evaluation Steps
1. Inspect the source image and identify its key visible content.
2. Review the RDF/Turtle graph and compare its triples against the image.
3. For each criterion, assess the graph using the rating scale and criterion guidelines.
4. Identify major errors that materially affect graph quality.
5. Return only the structured JSON object required by the response schema.
\end{lstlisting}

\subsection{Pairwise Judge Prompt}
\begin{lstlisting}[style=markdownstyle]
# Role
You are an evaluator comparing two RDF/Turtle knowledge graphs generated from the same source image. Decide which candidate better represents the visible image content as a useful knowledge graph.
# Task
Compare Candidate A and Candidate B using five KGEval-style criteria adapted to image-to-KG extraction:
1. Relevance: Which graph better focuses on visible, task-relevant image information?
2. Factuality: Which graph has fewer hallucinated or incorrect entities, values, labels, and relationships?
3. Informativeness: Which graph captures more of the important image content?
4. Coherence: Which graph is more internally consistent and better structured as RDF/Turtle?
5. Specificity: Which graph is more precise and less generic?
For each criterion, choose A, B, or tie. Then choose an overall winner. Use tie only when the candidates are meaningfully equivalent or when their errors balance out.
## Evaluation Steps
1. Inspect the source image and identify the key visible facts.
2. Analyze Candidate A and Candidate B independently.
3. Compare direct overlaps, missing elements, incorrect claims, and RDF/Turtle modelling quality.
4. Select a preference for each criterion.
5. Select the overall winner and confidence.
6. Return only the structured JSON object required by the response schema.
\end{lstlisting}

\bibliographystyle{elsarticle-num}
\bibliography{refs}

\end{document}
